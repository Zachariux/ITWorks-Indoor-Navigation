using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClassData : MonoBehaviour {
    public Node Location;
    public int xNodePos;
    public int yNodePos;
    enum Floor { B , G, F1, F2, F3, F4 };


    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
